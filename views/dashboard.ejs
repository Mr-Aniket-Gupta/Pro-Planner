<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Project Planner Dashboard</title>
    <!-- Tailwind CSS -->
    <link rel="preload" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
    </noscript>
    <!-- Charts, Alerts, Calendar, Fonts, Custom CSS -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/fullcalendar@5.11.3/main.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/css/dashboard.css">
    <!-- 3D, Animation, Physics Libraries -->
    <script type="module" src="https://unpkg.com/@google/model-viewer/dist/model-viewer.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/@motionone/dom/dist/motion.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/matter-js@0.19.0/build/matter.min.js" defer></script>
</head>

<body class="bg-gradient-to-br from-blue-50 to-white min-h-screen">
    <!-- Loader overlay (shows on top of all content) -->
    <div id="universalLoader" class="loader-overlay" style="display:none;">
        <div class="loader-spinner"></div>
    </div>

    <!-- Header (top navigation) -->
    <header class="flex items-center justify-between px-2 py-2 md:px-10 md:py-4 bg-white shadow-sm sticky top-0 z-10">
        <div class="flex items-center gap-2 min-w-0">
            <svg width="32" height="32" fill="none" viewBox="0 0 32 32">
                <rect width="32" height="32" rx="8" fill="#3B82F6" />
                <path d="M10 22l6-12 6 12" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
            <span class="text-lg md:text-2xl font-extrabold text-blue-600 tracking-tight truncate">Project Planner</span>
        </div>
        <div class="flex items-center gap-2 md:gap-4">
            <button onclick="window.location.href='/'" title="Go to Landing Page" class="p-2 rounded-full bg-blue-50 hover:bg-blue-100 text-blue-600 transition flex items-center justify-center">
                <svg width="18" height="18" fill="none" viewBox="0 0 18 18">
                    <path d="M3 9l6-6 6 6" stroke="#2563eb" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                    <rect x="5" y="9" width="8" height="6" rx="1" stroke="#2563eb" stroke-width="1.5" />
                </svg>
                <span class="hidden md:inline">Landing Page</span>
            </button>
            <!-- Connections button with badge -->
            <button id="connectionsBtn" title="Connections" class="relative p-2 rounded-full bg-blue-50 hover:bg-blue-100 text-blue-600 transition flex items-center justify-center">
                <svg width="22" height="22" fill="none" viewBox="0 0 24 24">
                    <path d="M17 21v-2a4 4 0 0 0-4-4H7a4 4 0 0 0-4 4v2" stroke="#2563eb" stroke-width="2" />
                    <circle cx="9" cy="7" r="4" stroke="#2563eb" stroke-width="2" />
                    <path d="M23 21v-2a4 4 0 0 0-3-3.87" stroke="#2563eb" stroke-width="2" />
                    <path d="M16 3.13a4 4 0 0 1 0 7.75" stroke="#2563eb" stroke-width="2" />
                </svg>
                <span id="connectionsBadge" class="absolute -top-1 -right-1 bg-red-500 text-white text-xs rounded-full px-1.5 py-0.5 font-bold hidden">0</span>
            </button>
            <button id="settingsBtn" title="Settings" class="p-2 rounded-full bg-blue-50 hover:bg-blue-100 text-blue-600 transition flex items-center justify-center">
                <svg width="22" height="22" fill="none" viewBox="0 0 24 24">
                    <path d="M12 15.5A3.5 3.5 0 1 0 12 8.5a3.5 3.5 0 0 0 0 7z" stroke="#2563eb" stroke-width="2" />
                    <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06A1.65 1.65 0 0 0 15 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 8.6 15a1.65 1.65 0 0 0-1.82-.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.6a1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 15 8.6a1.65 1.65 0 0 0 1.82.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 15z" stroke="#2563eb" stroke-width="2" />
                </svg>
            </button>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-2 md:px-8 py-8 flex flex-col gap-8">
        <!-- Top Section: Projects + Project Details -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <!-- Projects Card -->
            <section
                class="bg-white rounded-2xl shadow-xl p-6 flex flex-col gap-4 border border-blue-100 transition-all duration-300 hover:shadow-2xl">
                <div class="flex items-center justify-between mb-3">
                    <h2 class="text-xl font-extrabold text-blue-700 flex items-center gap-2 tracking-tight">
                        <svg width="22" height="22" fill="none" viewBox="0 0 22 22">
                            <rect width="22" height="22" rx="6" fill="#DBEAFE" />
                            <path d="M7 15l4-8 4 8" stroke="#3B82F6" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                        </svg>
                        Projects
                    </h2>
                    <button onclick="showProjectForm()"
                        class="bg-gradient-to-r from-blue-400 to-blue-600 text-white rounded-full p-2 shadow hover:from-blue-500 hover:to-blue-700 transition focus:outline-none focus:ring-2 focus:ring-blue-300"
                        title="Add Project">
                        <svg width="22" height="22" fill="none" viewBox="0 0 22 22">
                            <circle cx="11" cy="11" r="10" stroke="#fff" stroke-width="2" />
                            <path d="M11 7v8M7 11h8" stroke="#fff" stroke-width="2" stroke-linecap="round" />
                        </svg>
                    </button>
                </div>
                <!-- Project Search Bar -->
                <div class="mb-2">
                    <input type="text" id="projectSearch" placeholder="Search projects by name or tag..."
                        class="w-full border border-blue-200 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-300 text-blue-700 placeholder-blue-300 transition" />
                </div>
                <ul id="projectList" class="space-y-2 max-h-64 overflow-y-auto pr-1 custom-scrollbar"></ul>
                <div id="emptyProjectState" class="text-center text-blue-300 py-8">
                    <svg class="mx-auto mb-2" width="44" height="44" fill="none" stroke="currentColor">
                        <circle cx="22" cy="22" r="20" stroke-width="2" />
                        <path d="M12 22h20M22 12v20" stroke-width="2" />
                    </svg>
                    <div class="font-semibold">No projects yet</div>
                </div>
            </section>
            <!-- Project Details Card -->
            <section class="md:col-span-2 bg-white rounded-2xl shadow-lg p-6 flex flex-col gap-4 border border-blue-50"
                id="projectDetailsCard" style="display:none;">
                <div class="flex items-center justify-between mb-2">
                    <h2 class="text-lg font-bold text-blue-600 flex items-center gap-2">
                        <svg width="20" height="20" fill="none" viewBox="0 0 20 20">
                            <rect width="20" height="20" rx="5" fill="#DBEAFE" />
                            <path d="M6 14l4-8 4 8" stroke="#3B82F6" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                        </svg>
                        <span id="projectTitle">Project Name</span>
                    </h2>
                    <button id="editProjectBtn"
                        class="bg-blue-100 hover:bg-blue-200 text-blue-600 rounded-full p-2 transition"
                        title="Edit Project">
                        <svg width="18" height="18" fill="none" viewBox="0 0 18 18">
                            <path d="M3 12.75V15h2.25l6.62-6.62a1.5 1.5 0 00-2.12-2.12L3 12.75z" stroke="#3B82F6" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                            <path d="M14.25 5.25a1.5 1.5 0 00-2.12-2.12l-1.13 1.13a1.5 1.5 0 002.12 2.12l1.13-1.13z" stroke="#3B82F6" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
                        </svg>
                    </button>
                </div>
                <div class="text-gray-700 mb-2" id="projectDesc">Project description...</div>
                <div class="flex flex-wrap gap-4">
                    <div class="bg-blue-50 rounded-xl px-4 py-2 text-blue-700 text-sm font-medium" id="projectBasic">
                        Basic: -</div>
                    <div class="bg-blue-50 rounded-xl px-4 py-2 text-blue-700 text-sm font-medium" id="projectAdvanced">
                        Advanced: -</div>
                </div>
                <!-- New Features Start -->
                <div class="flex flex-wrap gap-4 items-center mt-2">
                    <button id="markCompleteBtn"
                        class="bg-green-100 hover:bg-green-200 text-green-700 rounded-xl px-4 py-2 font-semibold transition">Mark as Complete</button>
                    <button id="markIncompleteBtn"
                        class="bg-yellow-100 hover:bg-yellow-200 text-yellow-700 rounded-xl px-4 py-2 font-semibold transition"
                        style="display:none;">Mark as Incomplete</button>
                    <button id="exportProjectBtn"
                        class="bg-blue-100 hover:bg-blue-200 text-blue-700 rounded-xl px-4 py-2 font-semibold transition">Export as PDF</button>
                    <div class="flex items-center gap-2">
                        <label for="projectDeadline" class="text-blue-600 font-semibold">Deadline:</label>
                        <input type="date" id="projectDeadline" class="border border-blue-100 rounded-xl px-2 py-1"
                            onchange="saveProjectDeadline(projects[selectedProjectIndex]._id, this.value)" />
                    </div>
                </div>
                <div class="w-full mt-4">
                    <div class="flex items-center gap-2 mb-1">
                        <span class="text-blue-600 font-semibold">Progress:</span>
                        <span id="projectProgressPercent">0%</span>
                    </div>
                    <div class="w-full h-3 bg-blue-100 rounded-full overflow-hidden">
                        <div id="projectProgressBar" class="h-3 bg-blue-500 rounded-full transition-all"
                            style="width:0%"></div>
                    </div>
                </div>
                <div class="mt-4">
                    <label class="text-blue-600 font-semibold mb-1 block">Quick Notes:</label>
                    <textarea id="projectQuickNotes" rows="2"
                        class="w-full border border-blue-100 p-2 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200"
                        oninput="this.style.height='auto';this.style.height=(this.scrollHeight)+'px';"></textarea>
                    <button id="saveProjectNotesBtn"
                        class="mt-2 bg-blue-500 text-white px-3 py-1 rounded-xl font-semibold hover:bg-blue-600 transition">Save Notes</button>
                </div>
                <!-- New Features End -->
            </section>
        </div>

        <!-- Top Section: Task Management + Sidebar -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8" id="mainTaskDashboard">
            <!-- Task Management Card (Left, 2/3) -->
            <section
                class="md:col-span-2 bg-white rounded-2xl shadow-xl p-6 flex flex-col gap-4 border border-blue-100 transition-all duration-300 hover:shadow-2xl"
                id="taskSection">
                <!-- Task Search & Filters -->
                <div class="flex flex-col md:flex-row md:items-end gap-4 mb-3 overflow-x-auto flex-wrap">
                    <input type="text" id="taskSearch" placeholder="Search tasks by name or tag..."
                        class="border border-blue-200 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-300 flex-1 text-blue-700 placeholder-blue-300 transition" />
                    <select id="taskTagFilter"
                        onchange="renderTasks(document.getElementById('taskSearch').value, this.value)"
                        class="border border-blue-200 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200">
                        <option value="">All Tags</option>
                        <option value="General">General</option>
                        <option value="Frontend">Frontend</option>
                        <option value="Backend">Backend</option>
                        <option value="Bug">Bug</option>
                    </select>
                </div>
                <div class="flex flex-col md:flex-row md:items-end gap-4 mb-3 overflow-x-auto flex-wrap">
                    <input type="text" id="taskInput" placeholder="New Task"
                        class="border border-blue-200 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-300 flex-1 text-blue-700 placeholder-blue-300 transition" />
                    <select id="taskTag"
                        class="border border-blue-200 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200">
                        <option value="General">General</option>
                        <option value="Frontend">Frontend</option>
                        <option value="Backend">Backend</option>
                        <option value="Bug">Bug</option>
                    </select>
                    <select id="taskPriority"
                        class="border border-yellow-200 p-3 rounded-xl bg-yellow-50 focus:outline-none focus:ring-2 focus:ring-yellow-200">
                        <option value="Low">Low Priority</option>
                        <option value="Medium">Medium Priority</option>
                        <option value="High">High Priority</option>
                    </select>
                    <input type="date" id="taskDueDate"
                        class="border border-blue-200 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-300 min-w-[140px]"
                        placeholder="Due Date" />
                    <button id="addTaskBtn"
                        class="bg-gradient-to-r from-blue-400 to-blue-600 text-white px-4 py-2 rounded-xl font-bold shadow hover:from-blue-500 hover:to-blue-700 transition flex-shrink-0 w-full md:w-auto">Add Task</button>
                </div>
                <ul id="taskList" class="space-y-2 mb-2 custom-scrollbar"></ul>
                <div id="emptyTaskState" class="text-center text-blue-300 py-8">
                    <svg class="mx-auto mb-2" width="44" height="44" fill="none" stroke="currentColor">
                        <rect x="8" y="16" width="24" height="12" rx="4" stroke-width="2" />
                        <path d="M16 22h8M20 18v8" stroke-width="2" />
                    </svg>
                    <div class="font-semibold">No tasks found</div>
                </div>
            </section>
            <!-- Sidebar (Right, 1/3): Todo List + Notes -->
            <aside class="bg-white rounded-2xl shadow-lg p-6 flex flex-col gap-8 border border-blue-50 h-full">
                <!-- Todo List -->
                <div>
                    <h3 class="font-bold text-blue-600 mb-2 flex items-center gap-1">
                        <svg width="18" height="18" fill="none" viewBox="0 0 18 18">
                            <rect width="18" height="18" rx="4" fill="#DBEAFE" />
                            <path d="M5 13l4-8 4 8" stroke="#3B82F6" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round" />
                        </svg> My Todos
                    </h3>
                    <div class="flex gap-2 mb-2">
                        <input type="text" id="sidebarTodoInput" placeholder="Add todo..."
                            class="border border-blue-100 p-2 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200 flex-1" />
                        <button id="addSidebarTodoBtn"
                            class="bg-blue-500 text-white px-3 py-1 rounded-xl font-semibold hover:bg-blue-600 transition">Add</button>
                    </div>
                    <ul id="sidebarTodoList" class="space-y-2"></ul>
                </div>
                <!-- Notes -->
                <div>
                    <h3 class="font-bold text-blue-600 mb-2 flex items-center gap-1">
                        <svg width="18" height="18" fill="none" viewBox="0 0 18 18">
                            <rect width="18" height="18" rx="4" fill="#DBEAFE" />
                            <path d="M5 13l4-8 4 8" stroke="#3B82F6" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round" />
                        </svg> Notes
                    </h3>
                    <textarea id="sidebarNotes" rows="6" placeholder="Write your notes here..."
                        class="w-full border border-blue-100 p-2 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200"></textarea>
                    <button id="saveSidebarNotesBtn"
                        class="mt-2 bg-blue-500 text-white px-3 py-1 rounded-xl font-semibold hover:bg-blue-600 transition w-full">Save Notes</button>
                </div>
            </aside>
        </div>

        <div id="selectProjectMsg" class="text-center text-blue-400 text-lg font-semibold py-16 hidden">
            <svg class="mx-auto mb-4" width="48" height="48" fill="none" stroke="#3B82F6">
                <rect x="8" y="16" width="32" height="20" rx="4" stroke-width="2" />
                <path d="M16 24h16M24 20v8" stroke-width="2" />
            </svg>
            Please select a project to manage tasks.
        </div>

        <!-- Dashboard Overview Section -->
        <section class="w-full mt-10 overflow-x-hidden">
            <div class="mx-auto max-w-7xl px-2 md:px-6">
                <h2 class="text-2xl md:text-3xl font-extrabold text-blue-700 mb-10 tracking-tight text-center">Dashboard Overview</h2>
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
                    <!-- Existing Cards -->
                    <div
                        class="bg-gradient-to-br from-blue-100 to-blue-300 rounded-2xl shadow-xl p-6 flex flex-col items-center transition-transform hover:scale-105 min-h-[140px]">
                        <span class="text-3xl font-extrabold text-blue-700" id="summaryTotalProjects">0</span>
                        <span class="text-blue-700 font-bold mt-2 tracking-wide">Total Projects</span>
                    </div>
                    <div
                        class="bg-gradient-to-br from-green-100 to-green-300 rounded-2xl shadow-xl p-6 flex flex-col items-center transition-transform hover:scale-105 min-h-[140px]">
                        <span class="text-3xl font-extrabold text-green-700" id="summaryCompletedProjects">0</span>
                        <span class="text-green-700 font-bold mt-2 tracking-wide">Completed</span>
                    </div>
                    <div
                        class="bg-gradient-to-br from-yellow-100 to-yellow-300 rounded-2xl shadow-xl p-6 flex flex-col items-center transition-transform hover:scale-105 min-h-[140px]">
                        <span class="text-3xl font-extrabold text-yellow-700" id="summaryActiveProjects">0</span>
                        <span class="text-yellow-700 font-bold mt-2 tracking-wide">Active</span>
                    </div>
                    <div
                        class="bg-gradient-to-br from-red-100 to-red-300 rounded-2xl shadow-xl p-6 flex flex-col items-center transition-transform hover:scale-105 min-h-[140px]">
                        <span class="text-3xl font-extrabold text-red-700" id="summaryOverdueProjects">0</span>
                        <span class="text-red-700 font-bold mt-2 tracking-wide">Overdue</span>
                    </div>
                    <!-- New Cards -->
                    <div
                        class="bg-gradient-to-br from-blue-50 to-blue-200 rounded-2xl shadow-xl p-6 flex flex-col items-center min-h-[140px]">
                        <span class="text-3xl font-extrabold text-blue-700" id="summaryTotalTasks">0</span>
                        <span class="text-blue-700 font-bold mt-2 tracking-wide">Total Tasks</span>
                    </div>
                    <div
                        class="bg-gradient-to-br from-green-50 to-green-200 rounded-2xl shadow-xl p-6 flex flex-col items-center min-h-[140px]">
                        <span class="text-3xl font-extrabold text-green-700" id="summaryCompletedTasks">0</span>
                        <span class="text-green-700 font-bold mt-2 tracking-wide">Completed Tasks</span>
                    </div>
                    <div
                        class="bg-gradient-to-br from-yellow-50 to-yellow-200 rounded-2xl shadow-xl p-6 flex flex-col items-center min-h-[140px]">
                        <span class="text-3xl font-extrabold text-yellow-700" id="summaryPendingTasks">0</span>
                        <span class="text-yellow-700 font-bold mt-2 tracking-wide">Pending Tasks</span>
                    </div>
                    <div
                        class="bg-gradient-to-br from-pink-50 to-pink-200 rounded-2xl shadow-xl p-6 flex flex-col items-center min-h-[140px]">
                        <span class="text-3xl font-extrabold text-pink-700" id="summaryHighPriorityTasks">0</span>
                        <span class="text-pink-700 font-bold mt-2 tracking-wide">High Priority Tasks</span>
                    </div>
                </div>
                <!-- Charts Row -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-10">
                    <div class="bg-white rounded-2xl shadow-xl p-6 flex flex-col items-center">
                        <h3 class="font-bold text-blue-600 mb-2">Task Status</h3>
                        <canvas id="pieChart" width="220" height="220"></canvas>
                    </div>
                    <div class="bg-white rounded-2xl shadow-xl p-6 flex flex-col items-center">
                        <h3 class="font-bold text-blue-600 mb-2">Priority Distribution</h3>
                        <canvas id="doughnutChart" width="220" height="220"></canvas>
                    </div>
                </div>
                <div class="bg-white rounded-2xl shadow-xl p-6 flex flex-col items-center mt-8">
                    <h3 class="font-bold text-blue-600 mb-2">Daily Project Activity</h3>
                    <canvas id="lineChart" width="600" height="220"></canvas>
                </div>
                <!-- Upcoming Deadlines & Most Active Project -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-10 mt-8">
                    <div class="bg-white rounded-2xl shadow-xl p-6">
                        <h3 class="font-bold text-blue-600 mb-2">Upcoming Deadlines</h3>
                        <ul id="upcomingDeadlines" class="space-y-2"></ul>
                    </div>
                    <div class="bg-white rounded-2xl shadow-xl p-6">
                        <h3 class="font-bold text-blue-600 mb-2">Most Active Project</h3>
                        <div id="mostActiveProject" class="text-lg font-semibold text-blue-700"></div>
                    </div>
                </div>
                <!-- Recent Activity Feed -->
                <div class="bg-white rounded-2xl shadow-xl p-6 mb-10">
                    <h3 class="font-bold text-blue-600 mb-2">Recent Activity</h3>
                    <ul id="activityFeed" class="space-y-2"></ul>
                </div>
                <!-- Average Completion Rate -->
                <div class="bg-white rounded-2xl shadow-xl p-6 flex flex-col items-center mb-10">
                    <h3 class="font-bold text-blue-600 mb-2">Average Completion Rate</h3>
                    <div id="avgCompletionRate" class="text-3xl font-extrabold text-green-600">0%</div>
                </div>
            </div>
        </section>
    </main>

    <!-- Project Modal (hidden by default) -->
    <div id="projectModal" class="fixed inset-0 bg-black bg-opacity-30 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-2xl shadow-xl p-8 w-full max-w-md relative flex flex-col" style="max-height:90vh;">
            <button id="closeProjectFormBtn" class="absolute top-3 right-3 text-gray-400 hover:text-blue-500">
                <svg width="24" height="24" fill="none" viewBox="0 0 24 24">
                    <path d="M6 6l12 12M6 18L18 6" stroke="#3B82F6" stroke-width="2" stroke-linecap="round" />
                </svg>
            </button>
            <h2 class="text-xl font-bold text-blue-600 mb-4" id="projectModalTitle">Add Project</h2>
            <form id="projectFormEl" class="space-y-4 flex-1 overflow-y-auto pr-2" style="max-height:60vh;">
                <!-- Make this project private option -->
                <div id="makeProjectPrivateDiv">
                    <label class="inline-flex items-center mt-1">
                        <input type="checkbox" id="projectIsPrivate" class="form-checkbox h-5 w-5 text-blue-600">
                        <span class="ml-2 text-blue-700">Make this project private</span>
                    </label>
                </div>
                <input type="text" id="projectName" placeholder="Project Name"
                    class="w-full border border-blue-100 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200"
                    required />
                <textarea id="projectDescInput" placeholder="Project Description"
                    class="w-full border border-blue-100 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200"
                    required></textarea>

                <div>
                    <label class="block text-blue-500 font-semibold mb-1">Basic Requirements</label>
                    <div id="basicInputs" class="space-y-2">
                        <input type="text"
                            class="w-full border border-blue-100 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200 basic-input"
                            placeholder="Basic Requirement" />
                    </div>
                    <button type="button" id="addBasicInputBtn" class="mt-2 text-xs text-blue-500 hover:underline">+ Add More</button>
                </div>
                <div>
                    <label class="block text-blue-500 font-semibold mb-1">Advanced Features</label>
                    <div id="advancedInputs" class="space-y-2">
                        <input type="text"
                            class="w-full border border-blue-100 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200 advanced-input"
                            placeholder="Advanced Feature" />
                    </div>
                    <button type="button" id="addAdvancedInputBtn" class="mt-2 text-xs text-blue-500 hover:underline">+ Add More</button>
                </div>
                <div>
                    <label class="block text-blue-500 font-semibold mb-1">Quick Notes</label>
                    <textarea id="projectQuickNotes" rows="2"
                        class="w-full border border-blue-100 p-3 rounded-xl bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-200 resize-none"
                        placeholder="Add quick notes about this project..."></textarea>
                </div>
            </form>
            <div class="pt-4 bg-white sticky bottom-0 left-0 right-0 z-10">
                <button type="submit" form="projectFormEl"
                    class="bg-gradient-to-r from-blue-400 to-blue-600 text-white px-4 py-2 rounded-xl font-semibold shadow hover:from-blue-500 hover:to-blue-700 transition w-full">Save Project</button>
            </div>
        </div>
    </div>

    <!-- Modern Settings Modal -->
    <div id="settingsHubModal" class="fixed right-0 left-0 top-[64px] bg-white z-[100] hidden" style="top:64px; height:calc(100vh - 64px); transform: translateX(100%); transition: transform 0.3s ease-in-out;">
        <div class="flex h-full">
            <!-- Sidebar -->
            <aside class="w-16 md:w-64 bg-gray-50 border-r border-gray-200 flex flex-col">
                <div class="p-4 border-b border-gray-200 flex items-center justify-between gap-2">
                    <h2 class="text-lg font-bold text-gray-800 hidden md:block">Settings</h2>
                    <button id="closeSettingsHubBtn" class="p-2 rounded-full hover:bg-gray-200">
                        <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                    </button>
                </div>
                <nav id="settingsHubNav" class="flex-1 space-y-2 p-2">
                    <!-- Nav items will be injected by JS -->
                </nav>
            </aside>
            <!-- Main Content -->
            <main id="settingsHubContent" class="flex-1 p-4 md:p-6 overflow-y-auto bg-white text-gray-800">
                <!-- Social Media Section -->
                <div id="settingsSocialSection" class="mb-8">
                    <h3 class="text-lg font-bold text-blue-700 mb-3 flex items-center gap-2">
                        <svg width="20" height="20" fill="none" viewBox="0 0 20 20"><circle cx="10" cy="10" r="9" stroke="#2563eb" stroke-width="2"/><path d="M7 10h6M10 7v6" stroke="#2563eb" stroke-width="2" stroke-linecap="round"/></svg>
                        Social Media & Project Links
                    </h3>
                    <div id="socialLinksList" class="space-y-3">
                        <!-- Social link rows will be injected by JS -->
                    </div>
                    <button id="addMoreSocialBtn" type="button" class="mt-3 bg-blue-100 hover:bg-blue-200 text-blue-700 px-4 py-2 rounded-xl font-semibold transition flex items-center gap-2">
                        <svg width="18" height="18" fill="none" viewBox="0 0 18 18"><circle cx="9" cy="9" r="8" stroke="#2563eb" stroke-width="2"/><path d="M9 5v8M5 9h8" stroke="#2563eb" stroke-width="2" stroke-linecap="round"/></svg>
                        Add More Social
                    </button>
                </div>
                <!-- Other settings content will be injected by JS -->
            </main>
        </div>
    </div>

    <!-- AI Bot Modal -->
    <div id="aiBotModal" class="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-2xl shadow-xl w-full max-w-2xl relative flex flex-col" style="max-height:90vh;">
            <!-- Header -->
            <div class="flex items-center justify-between p-6 border-b border-gray-200">
                <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-gradient-to-r from-purple-400 to-purple-600 rounded-full flex items-center justify-center">
                        <svg width="20" height="20" fill="none" viewBox="0 0 24 24">
                            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" fill="#fff" />
                            <circle cx="12" cy="12" r="3" fill="#fff" />
                        </svg>
                    </div>
                    <div>
                        <h2 class="text-xl font-bold text-purple-600">ProPlanner AI Assistant</h2>
                        <p class="text-sm text-gray-500">Text & Voice Chat Available</p>
                    </div>
                </div>
                <button id="closeAiBotModalBtn" class="text-gray-400 hover:text-purple-500">
                    <svg width="24" height="24" fill="none" viewBox="0 0 24 24">
                        <path d="M6 6l12 12M6 18L18 6" stroke="#9333ea" stroke-width="2" stroke-linecap="round" />
                    </svg>
                </button>
            </div>

            <!-- Chat Area -->
            <div id="aiChatArea" class="flex-1 p-6 overflow-y-auto max-h-96 space-y-4">
                <div class="flex items-start gap-3">
                    <div class="w-8 h-8 bg-purple-100 rounded-full flex items-center justify-center flex-shrink-0">
                        <svg width="16" height="16" fill="none" viewBox="0 0 24 24">
                            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" fill="#9333ea" />
                        </svg>
                    </div>
                    <div class="bg-purple-50 rounded-xl p-3 max-w-xs">
                        <p class="text-sm text-purple-700">नमस्ते! मैं आपका ProPlanner AI Assistant हूं। मैं आपकी project planning और task management में मदद कर सकता हूं। आप text या voice से मुझसे बात कर सकते हैं!</p>
                    </div>
                </div>
            </div>

            <!-- Input Area -->
            <div class="p-6 border-t border-gray-200">
                <div class="flex gap-3">
                    <div class="flex-1 relative">
                        <input type="text" id="aiMessageInput" placeholder="Type your message here..."
                            class="w-full border border-purple-200 p-3 pr-12 rounded-xl bg-purple-50 focus:outline-none focus:ring-2 focus:ring-purple-300 text-purple-700 placeholder-purple-400" />
                        <button id="voiceBtn" type="button"
                            class="absolute right-3 top-1/2 transform -translate-y-1/2 text-purple-500 hover:text-purple-700 transition">
                            <svg id="micIcon" width="20" height="20" fill="none" viewBox="0 0 24 24">
                                <path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z" stroke="currentColor" stroke-width="2" />
                                <path d="M19 10v2a7 7 0 0 1-14 0v-2" stroke="currentColor" stroke-width="2" />
                                <line x1="12" y1="19" x2="12" y2="23" stroke="currentColor" stroke-width="2" />
                                <line x1="8" y1="23" x2="16" y2="23" stroke="currentColor" stroke-width="2" />
                            </svg>
                            <svg id="stopIcon" width="20" height="20" fill="none" viewBox="0 0 24 24" style="display:none;">
                                <rect x="6" y="6" width="12" height="12" rx="2" stroke="currentColor" stroke-width="2" />
                            </svg>
                        </button>
                    </div>
                    <button id="sendAiMessageBtn"
                        class="bg-gradient-to-r from-purple-400 to-purple-600 text-white px-6 py-3 rounded-xl font-semibold shadow hover:from-purple-500 hover:to-purple-700 transition flex items-center gap-2">
                        <svg width="18" height="18" fill="none" viewBox="0 0 24 24">
                            <path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                        </svg>
                        Send
                    </button>
                </div>
                <div id="voiceStatus" class="text-center text-sm text-purple-600 mt-2 hidden">
                    <span id="voiceStatusText">Click microphone to start voice input</span>
                </div>
            </div>
        </div>
    </div>

    <!-- Floating AI Bot Button (bottom-right) -->
    <div id="aiBotBtnWrapper" style="position:fixed; bottom:32px; right:32px; z-index:1000;">
        <button id="aiBotBtn" title="AI Assistant - Text & Voice Chat"
            class="px-3 py-3 rounded-full bg-purple-600 hover:bg-purple-700 text-white font-bold shadow-lg flex items-center gap-1 transition"
            style="box-shadow:0 4px 24px #9333ea44;">
            <svg width="28" height="28" fill="none" viewBox="0 0 24 24">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z" fill="#fff" />
                <circle cx="12" cy="12" r="3" fill="#fff" />
            </svg>
        </button>
    </div>

    <!-- OTP Modal -->
    <div id="otpModal" class="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-2xl shadow-xl p-8 w-full max-w-xs relative flex flex-col items-center">
            <button id="closeOtpModalBtn" class="absolute top-3 right-3 text-gray-400 hover:text-blue-500">
                <svg width="24" height="24" fill="none" viewBox="0 0 24 24">
                    <path d="M6 6l12 12M6 18L18 6" stroke="#3B82F6" stroke-width="2" stroke-linecap="round" />
                </svg>
            </button>
            <h2 class="text-lg font-bold text-blue-600 mb-4">Verify Email</h2>
            <p class="text-gray-600 mb-2" id="otpEmailText"></p>
            <input type="text" id="otpInput" maxlength="6" placeholder="Enter OTP"
                class="w-full border border-blue-100 p-3 rounded-xl bg-blue-50 mb-3" />
            <button id="verifyOtpBtn"
                class="w-full bg-gradient-to-r from-blue-400 to-blue-600 text-white px-4 py-2 rounded-xl font-semibold shadow hover:from-blue-500 hover:to-blue-700 transition">Verify</button>
            <div class="mt-3 text-sm text-gray-500">
                <span id="otpTimer"></span>
                <button id="resendOtpBtn" class="ml-2 text-blue-500 hover:underline" style="display:none;">Resend OTP</button>
            </div>
        </div>
    </div>

    <!-- Connections Hub (Full-Screen Modal) -->
    <div id="connectionsHubModal" class="fixed right-0 bottom-0 left-0 bg-white z-[100] hidden" style="transform: translateX(100%); transition: transform 0.3s ease-in-out;">
        <div class="flex h-full">
            <!-- Sidebar -->
            <aside class="w-16 md:w-64 bg-gray-50 border-r border-gray-200 flex flex-col">
                <div class="p-4 border-b border-gray-200 flex items-center justify-between gap-2">
                    <h2 class="text-lg font-bold text-gray-800 hidden md:block">Connections Hub</h2>
                    <button id="closeConnectionsHubBtn" class="p-2 rounded-full hover:bg-gray-200">
                        <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                    </button>
                </div>
                <nav id="connectionsHubNav" class="flex-1 space-y-2 p-2">
                    <!-- Nav items will be injected by JS -->
                </nav>
            </aside>
            <!-- Main Content -->
            <main id="connectionsHubContent" class="flex-1 p-4 md:p-6 overflow-y-auto bg-white text-gray-800">
                <!-- Content will be injected by JS -->
            </main>
        </div>
    </div>

    <!-- Friend Chat Modal (Dashboard Chat App) -->
    <div id="friendChatModal" class="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-3xl flex flex-col md:flex-row relative"
            style="max-height:90vh; min-height:400px; min-width:320px;">
            <!-- Close Button -->
            <button id="closeFriendChatModalBtn" class="absolute top-3 right-3 text-gray-400 hover:text-blue-500 z-10">
                <svg width="24" height="24" fill="none" viewBox="0 0 24 24">
                    <path d="M6 6l12 12M6 18L18 6" stroke="#3B82F6" stroke-width="2" stroke-linecap="round" />
                </svg>
            </button>
            <!-- Friends List Sidebar (LinkedIn style) -->
            <aside
                class="bg-blue-50 md:w-80 w-full md:h-auto h-24 rounded-l-2xl flex flex-col overflow-y-auto border-r border-blue-100">
                <div class="p-4 font-bold text-blue-600 text-lg border-b border-blue-100 flex items-center gap-2">
                    <svg width="22" height="22" fill="none" viewBox="0 0 24 24">
                        <circle cx="12" cy="12" r="10" stroke="#2563eb" stroke-width="2" />
                        <path d="M16 17v-1a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v1" stroke="#2563eb" stroke-width="2" />
                        <circle cx="12" cy="9" r="3" stroke="#2563eb" stroke-width="2" />
                    </svg>
                    Messaging
                </div>
                <!-- Search bar -->
                <div class="px-4 py-2 border-b border-blue-100 bg-blue-50">
                    <input type="text" id="chatFriendSearch" placeholder="Search messages"
                        class="w-full border border-blue-200 rounded-xl px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-300 text-blue-700 placeholder-blue-300 transition" />
                </div>
                <!-- Friend list -->
                <ul id="chatFriendsList" class="flex-1 overflow-y-auto divide-y divide-blue-100">
                    <!-- Example friend item (JS se render hoga):
                    <li class="flex items-center gap-3 px-4 py-3 cursor-pointer hover:bg-blue-100 transition relative">
                        <div class="relative">
                            <img src="/img/avatar1.png" alt="Avatar" class="w-10 h-10 rounded-full object-cover border-2 border-white shadow" />
                            <span class="absolute bottom-0 right-0 w-3 h-3 bg-green-400 border-2 border-white rounded-full"></span>
                        </div>
                        <div class="flex-1 min-w-0">
                            <div class="font-semibold text-blue-800 truncate">Aniket Gupta</div>
                            <div class="text-sm text-gray-500 truncate">You: Thanks a lot</div>
                        </div>
                        <div class="text-xs text-gray-400 ml-2 whitespace-nowrap">Jul 15</div>
                    </li>
                    -->
                </ul>
            </aside>
            <!-- Chat Area (LinkedIn style) -->
            <section class="flex-1 flex flex-col bg-white">
                <!-- Chat Header -->
                <div class="p-4 border-b border-blue-100 flex items-center gap-3">
                    <!-- <img id="chatFriendAvatar" src="/img/avatar1.png" alt="Avatar"
                        class="w-10 h-10 rounded-full object-cover border-2 border-white shadow"
                        style="display:none;" /> -->
                    <div>
                        <div id="chatFriendName" class="font-bold text-blue-700 text-lg truncate">Select a friend</div>
                        <div id="chatFriendStatus" class="text-xs text-gray-500">&nbsp;</div>
                    </div>
                </div>
                <!-- Chat Messages -->
                <div id="chatMessages" class="flex-1 p-4 overflow-y-auto space-y-3 bg-blue-25">
                </div>
                <!-- Message Input -->
                <form id="chatMessageForm" class="flex gap-2 p-4 border-t border-blue-100 bg-white">
                    <input id="chatMessageInput" type="text" placeholder="Write a message..." autocomplete="off"
                        class="flex-1 border border-blue-200 rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-300" />
                    <button type="submit"
                        class="bg-blue-500 hover:bg-blue-600 text-white rounded-xl px-4 py-2 font-semibold transition">Send</button>
                </form>
            </section>
        </div>
    </div>
    <!-- Floating Chat Button -->
    <div id="openFriendChatBtnWrapper" style="position:fixed; bottom:96px; right:32px; z-index:1000;">
        <button id="openFriendChatBtn" title="Chat with Friends"
            class="px-3 py-3 rounded-full bg-blue-600 hover:bg-blue-700 text-white font-bold shadow-lg flex items-center gap-1 transition"
            style="box-shadow:0 4px 24px #2563eb44;">
            <svg width="28" height="28" fill="none" viewBox="0 0 24 24">
                <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" fill="#fff" />
            </svg>
        </button>
    </div>

    <!-- jsPDF, dashboard scripts, and other JS libraries -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js" defer></script>
    <script src="/js/dashboard.js" defer></script>
    <script src="/js/ai.js" defer></script>
    <script src="/js/connections.js" defer></script>
    <script src="/js/DashboardOverview.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script>
        // Settings Modal JS - Wait for dashboard.js to load
        document.addEventListener('DOMContentLoaded', function () {
            // If openSettingsModal is available, attach to settings button
            if (typeof openSettingsModal === 'function') {
                const settingsBtn = document.getElementById('settingsBtn');
                if (settingsBtn) {
                    settingsBtn.onclick = openSettingsModal;
                }
            } else {
                console.log('openSettingsModal function not available yet');
            }
        });
    </script>

</body>

</html>